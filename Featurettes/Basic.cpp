// C++ BoilerPlate :

    // #include<iostream>
    // using namespace std;

    // int main(){
    
    // }


// Data Types : int, float, long, long long, double, char, with string and getline
// Arrays : collection of multiple data types given within []
// if(){ cout << ... ; }    else if(){ cout << ... ; }      else{ cout << ... ; }
// switch(data type value that we have given) { case 1 : cout << ... ; break;    ...    case n : cout << ... ; break;       default : cout << ... ; }
// for(initialisation, condition, increment/decrement) { cout << ... ; }
// initialisation; while(condition){ cout << ... ; } increment/decrement;
// initialisation; do{ cout << ... ; increment/decrement; } while(condition);


// Functions :
    // They are set of code which performs something for us.
    // They are used to modularise code.
    // They are used to increase readability.
    // They are used to use same code multiple times.

// Types Of Functions :
    // void --> which does not returns anything
    // return
    // parameterised
    // non parameterised

// pass by value --> makes a copy and pass it  --> function(data type   value){} int main(){}
// pass by reference --> it will change the original  --> function( data type   &value)  int main(){}
        // arrays always give the pass by reference